{
  "name": "retail360-root",
  "private": true,
  "version": "1.0.0",
  "description": "Proyecto raíz. El frontend React vive en ./frontend y el backend en ./backend.",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview"
  }
}

